# P02 · Migration 1: enums, configuração, usuários, família e pessoas

Data: 25/09/2026 (relatório escrito na integração da trilha de banco)
Branch e commits: `claude/kraamzorg-delivery-review-6kzd8q`. `0002_enums_config_familia.sql` e `supabase/tests/001_enums_config_familia.sql` entraram no commit de segurança `d584e8c`.

## Feito

1. **Enums**: os 49 enums do PRD 6.0 numa migration só, com as mudanças
   [v4.2] (conferidos um a um pelo `004_estrutura_modelo.sql`).
2. **Tabelas do PRD 6.1 e 6.2**, com `perfil` e `usuario_papel` primeiro:
   `perfil`, `usuario_papel`, `regiao`, `cidade`, `municipio`, `parametro`,
   `familia`, `pessoa`, `pessoa_dados_contrato`, `bebe` e `medico`. Colunas
   padrão (`id`, `criado_em`, `atualizado_em`, `criado_por` com referência a
   `perfil`) em todas menos as exceções do PRD 5.2 (`parametro`, `perfil`,
   `usuario_papel`, `municipio`).
3. `privado.tocar_atualizado_em()` e o gatilho em toda tabela com
   `atualizado_em`.
4. Índice em toda chave estrangeira.
5. RLS ligada em todas as tabelas, sem política (tudo negado até o P07).
6. `public.ig(dpp, data)` imutável: semanas e dias como inteiros e o texto
   `38s2d` (PRD 6.10 regra 7).
7. `comment on` em tabelas e colunas com o texto do PRD.
8. Testes: `001_enums_config_familia.sql` (10 asserções: RLS, colunas padrão
   e `ig()` com três casos, um com a DPP no passado) e a conferência cruzada
   `004_estrutura_modelo.sql` (58 asserções, junto com o P03 e o P04).

## Ficou de fora (e por quê)

- Políticas de acesso: P07 (`0007_permissoes.sql`).
- Dados: P08 (`seed.sql`).
- Auditoria genérica e `privado.transicionar()`: P05 e P06.

## Decisões tomadas nesta sessão

- Onde o PRD diz `-- padrão`, entram as quatro colunas exatamente como o
  P02 define, e só nas tabelas fora das exceções do PRD 5.2.
- Idade gestacional nunca é coluna: só `ig(dpp, data)`.
- `familia.historico_sensivel` é booleano, sem detalhe, como o PRD 6.2
  [v4.2] manda; quem vê ficou para o P07 (padrão: coordenação e diretoria).

## Mudanças no PRD

Nenhuma.

## Pendências novas ([confirmar], [clínico], terceiros)

- `familia.historico_sensivel`: quem vê na ficha [confirmar: Edilaine]
  (ADR 0002, pendências).
- Revisão humana do SQL antes do `db push` (parada prevista no PROMPTS.md).

## Como testar

Sem Docker (esta máquina), na raiz do repositório:

```bash
cd supabase/sem-docker
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/iniciar.sh    # sobe o Postgres 16 local (idempotente)
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/testar.sh     # reset do zero + seeds do config.toml + pg_prove
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/parar.sh
cd ../..
node supabase/checar-seed.mjs
```

Um arquivo só, depois de um `scripts/resetar.sh` (o `000_harness.sql` cria o
schema `testes` que os outros usam):

```bash
sudo -u postgres pg_prove --host 127.0.0.1 --port 54342 --dbname kraamzorg --username postgres \
  supabase/tests/000_harness.sql supabase/tests/ARQUIVO.sql
```

Com Docker (prova final antes de qualquer `db push`, `supabase/LIGAR.md`):

```bash
supabase start
supabase db reset     # migrations + [db.seed] de supabase/config.toml
supabase test db
```

## Resultado dos invariantes

Reset do zero e suíte inteira na integração da trilha de banco (porta 54342,
`PGDATA=/tmp/kz-pg-final`, seeds de `[db.seed]`): `Files=17, Tests=1971,
Result: PASS`. Invariante 1 (máquinas de estado): `006_maquinas_estado.sql`
verde. Invariante 2 (permissões): `007_permissoes.sql` e `015_crm_apoio.sql`
verdes. Invariante 3 (freio): `009_freio.sql` verde. Invariante 4
(sincronização offline) é do app (P12, `pnpm e2e:offline`) e não foi rodado
nesta integração, que só mexeu no banco.
