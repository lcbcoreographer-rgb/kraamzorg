# P03 · Migration 2: comercial, conversa, handoff, tarefas e linha do tempo

Data: 25/09/2026 (relatório escrito na integração da trilha de banco)
Branch e commits: `claude/kraamzorg-delivery-review-6kzd8q`. `0003_comercial_conversa.sql` e `supabase/tests/002_comercial_conversa.sql` entraram no commit de segurança `b123584`.

## Feito

1. **Tabelas do PRD 6.3 e 6.4**: `pacote`, `pacote_versao` (com exclusão de
   vigência sobreposta por `btree_gist`), `condicao_comercial`,
   `oportunidade` (índice único parcial: uma oportunidade aberta por
   família), `sessao_venda`, `sessao_venda_gravacao` (separada de
   `sessao_venda`), `contrato`, `cobranca`, `nota_fiscal`, `conversa`,
   `mensagem`, `handoff`, `tarefa` e `evento_familia`.
2. **`evento_familia` append-only** [v4.2]: `update`, `delete` e `truncate`
   revogados de todos os papéis, gatilho de recusa de linha
   (`privado.recusar_update_delete`) e gatilho de instrução antes do
   `truncate` (`privado.recusar_truncate`), que também barra
   `truncate familia cascade` rodando como `postgres`.
3. **`mensagem`**: fora da lista de append-only, mas `update` só na coluna
   `transcricao` (`privado.mensagem_so_transcricao`, PRD 5.2 [v4.2]).
4. Índices, comentários e RLS ligada, como no P02.
5. Teste `002_comercial_conversa.sql` (10 asserções): duas versões vigentes
   do mesmo pacote na mesma data, duas oportunidades abertas para a mesma
   família, `update` e `truncate` em `evento_familia` (inclusive
   `truncate familia cascade` como `postgres`), todos recusados.

## Ficou de fora (e por quê)

- Políticas: P07. Máquina de estado: P06. Auditoria: P05.
- A única exceção do append-only de `evento_familia` (troca de `titulo` e
  `dados` por `[eliminado]` na eliminação a pedido do titular) chegou no
  P22 (`0014_agente_parte2.sql`), liberada só por `app.eliminacao` dentro de
  `privado.eliminar_titular`.

## Decisões tomadas nesta sessão

- Os dois gatilhos de recusa são genéricos e reaproveitados no P04
  (`registro_atendimento`, `registro_adendo`) e no P05 (`log_auditoria`).
- O `truncate` é barrado por gatilho de instrução, e não só por privilégio,
  porque o dono (`postgres`) passa por cima de privilégio.

## Mudanças no PRD

Nenhuma.

## Pendências novas ([confirmar], [clínico], terceiros)

- Revisão humana do SQL antes do `db push` (parada prevista no PROMPTS.md).

## Como testar

Sem Docker (esta máquina), na raiz do repositório:

```bash
cd supabase/sem-docker
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/iniciar.sh    # sobe o Postgres 16 local (idempotente)
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/testar.sh     # reset do zero + seeds do config.toml + pg_prove
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/parar.sh
cd ../..
node supabase/checar-seed.mjs
```

Um arquivo só, depois de um `scripts/resetar.sh` (o `000_harness.sql` cria o
schema `testes` que os outros usam):

```bash
sudo -u postgres pg_prove --host 127.0.0.1 --port 54342 --dbname kraamzorg --username postgres \
  supabase/tests/000_harness.sql supabase/tests/ARQUIVO.sql
```

Com Docker (prova final antes de qualquer `db push`, `supabase/LIGAR.md`):

```bash
supabase start
supabase db reset     # migrations + [db.seed] de supabase/config.toml
supabase test db
```

## Resultado dos invariantes

Reset do zero e suíte inteira na integração da trilha de banco (porta 54342,
`PGDATA=/tmp/kz-pg-final`, seeds de `[db.seed]`): `Files=17, Tests=1971,
Result: PASS`. Invariante 1 (máquinas de estado): `006_maquinas_estado.sql`
verde. Invariante 2 (permissões): `007_permissoes.sql` e `015_crm_apoio.sql`
verdes. Invariante 3 (freio): `009_freio.sql` verde. Invariante 4
(sincronização offline) é do app (P12, `pnpm e2e:offline`) e não foi rodado
nesta integração, que só mexeu no banco.
