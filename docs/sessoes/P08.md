# P08 · Seed sintético, parâmetros e textos em rascunho

Data: 25/09/2026 (relatório escrito na integração da trilha de banco)
Branch e commits: `claude/kraamzorg-delivery-review-6kzd8q`. `supabase/seed.sql`, `supabase/checar-seed.mjs`, `supabase/dados/municipios_ibge.csv` e `supabase/tests/008_seed.sql` entraram no commit de segurança `905fc88`; os parâmetros acrescentados pelo P20 e pelo P22 e as quatro asserções da base de conhecimento ainda não têm commit.

## Feito

1. **Geografia**: 3 regiões, 14 cidades com aliases e taxa em centavos
   (Alphaville sem taxa; Granja Viana, Santo André, São Bernardo e São
   Caetano com taxa e `requer_confirmacao` onde o PRD 3.3 manda; Apucarana e
   Arapongas; Campinas e Sorocaba como não atendidas) e municípios do IBGE
   com a região intermediária.
2. **Pacotes**: os cinco planos com a versão vigente desde 01/03/2026 e uma
   versão anterior fictícia, encerrada (10 versões). Condições comerciais
   3x sem juros e Pix 5% com `requer_aprovacao` (C-04).
3. **Parâmetros** (36 no fechamento): os do agente (`agente_modo = teste`,
   `pdf_reenvio_janela_horas = 0`, `taxa_visivel_agente`,
   `alerta_internacao_ativo` e `alerta_emocional_ativo` falsos,
   `validador_listas`), `score_pesos` e `score_cortes` (70 e 40),
   `capacidade_alerta_pct` (85), `janela_dpp_dias` (21 dias antes e 14 depois da DPP),
   `handoff_matriz` do 11.4, expediente comercial, retenção, prazos,
   `freio_desfazer_segundos`, `acesso_enfermeira_pos_encerramento_dias` e os
   que o P20 e o P22 acrescentaram (`sessao_sem_agenda_horas`,
   `automacao_rota_interna`, `handoff_dedup_minutos`, `link_ficha_modelo`,
   `agente_followup_contexto_mensagens`, `handoff_motivos_legiveis`).
4. **`mensagem_modelo`** (88 textos): capítulo 23 inteiro e os textos padrão
   da evolução com prefixo `evo_`; `alerta_saude`, `perda` e
   `fallback_confirmar` aprovados, o resto em rascunho.
5. **`regua_faixa`** (5) e **`termo_alerta`** (20: os aprovados ativos, os
   sinônimos propostos inativos), com ação e mensagem.
6. **`automacao`**: catálogo do 10.1 (29), Fase 1 ativa, Fases 2 e 3
   inativas. **`regra_alerta`** (38): condição só onde o Apêndice B cita o
   DOC 3; as demais nulas e inativas até a Edilaine aprovar.
7. **Dados do 16.3**: um perfil de teste por papel
   (`*.teste@kraamzorgbrasil.test`), profissionais fictícias, doze famílias
   em todos os estágios (uma gemelar, uma em `bloqueio_total`, uma em
   `atencao`), um acompanhamento com seis visitas e registros, um alerta
   imediato fechado com conduta e sete conversas fictícias, uma por modo do
   agente que depende da conversa. Nomes com "Teste", telefones
   `+5511900000XXX`.
8. **`node supabase/checar-seed.mjs`**: falha com nome ou telefone fora do
   padrão fictício (PROMPTS.md pediu sem `pnpm seed:check`).
9. **Integração (25/09)**: a base de conhecimento inicial do agente
   (`supabase/dados/base_conhecimento_seed.sql`, 29 itens em rascunho,
   escrita pela trilha do n8n, P26) entra no seed de desenvolvimento por
   `[db.seed] sql_paths` do `supabase/config.toml`, depois do `seed.sql`,
   sem copiar nada para dentro do `seed.sql`. `resetar.sh` usa a mesma
   lista.
10. Teste `008_seed.sql` (83 asserções): versão vigente por pacote, toda
    `mensagem_chave` de `regua_faixa` e `termo_alerta` existe, parâmetros,
    textos, automações, perfis, famílias, acompanhamento, alerta, conversas
    e, desde a integração, a base de conhecimento (carregada, toda em
    rascunho, com fonte e sem item repetido).

## Ficou de fora (e por quê)

- Definições JSON completas dos instrumentos: P34 (`0045_instrumentos_v1.sql`).
- Seed de homologação com todos os textos aprovados: fica para a carga de
  hml (`supabase/LIGAR.md`, passo 7).
- Carga de configuração de produção (a parte do seed sem dado sintético):
  pendência registrada em `supabase/LIGAR.md`, passo 10.

## Decisões tomadas nesta sessão

- Municípios: a API do IBGE estava bloqueada nesta rede; o CSV é um
  subconjunto compilado à mão (Grande São Paulo e região de Londrina, as
  citadas no PRD 3.3), marcado assim no próprio arquivo. `scripts/baixar-municipios.mjs`
  gera a lista completa quando a rede permitir.
- O seed roda como dono, então pode inserir oportunidade, acompanhamento e
  visita já no estágio final da história e gravar `criado_por` à mão.
- Nenhum texto sobe para aprovado no seed de desenvolvimento além dos três
  que o PRD 6.8 manda.

## Mudanças no PRD

Nenhuma.

## Pendências novas ([confirmar], [clínico], terceiros)

- Taxa do ABC: v4.0 sem taxa, onboarding R$ 350 (C-01) [confirmar].
- Pesos internos e critérios do `score_pesos`, `limiar_nome` e
  `nova_gestacao_dias` da deduplicação [confirmar: Leonardo].
- Expediente comercial, pausas do agente, janela de envio e
  `agente_followup_horas` [confirmar: Leonardo].
- Retenção (O-06) e retenção de áudio (O-03) [confirmar: Leonardo e
  jurídico].
- Destino de mídia e áudio de cliente na `handoff_matriz` [confirmar:
  Edilaine].
- Baixar a lista completa de municípios do IBGE.
- Separar a carga de configuração de produção do `seed.sql`.

## Como testar

Sem Docker (esta máquina), na raiz do repositório:

```bash
cd supabase/sem-docker
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/iniciar.sh    # sobe o Postgres 16 local (idempotente)
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/testar.sh     # reset do zero + seeds do config.toml + pg_prove
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/parar.sh
cd ../..
node supabase/checar-seed.mjs
```

Um arquivo só, depois de um `scripts/resetar.sh` (o `000_harness.sql` cria o
schema `testes` que os outros usam):

```bash
sudo -u postgres pg_prove --host 127.0.0.1 --port 54342 --dbname kraamzorg --username postgres \
  supabase/tests/000_harness.sql supabase/tests/ARQUIVO.sql
```

Com Docker (prova final antes de qualquer `db push`, `supabase/LIGAR.md`):

```bash
supabase start
supabase db reset     # migrations + [db.seed] de supabase/config.toml
supabase test db
```

## Resultado dos invariantes

Reset do zero e suíte inteira na integração da trilha de banco (porta 54342,
`PGDATA=/tmp/kz-pg-final`, seeds de `[db.seed]`): `Files=17, Tests=1971,
Result: PASS`. Invariante 1 (máquinas de estado): `006_maquinas_estado.sql`
verde. Invariante 2 (permissões): `007_permissoes.sql` e `015_crm_apoio.sql`
verdes. Invariante 3 (freio): `009_freio.sql` verde. Invariante 4
(sincronização offline) é do app (P12, `pnpm e2e:offline`) e não foi rodado
nesta integração, que só mexeu no banco.
