# P04 · Migration 3: operação, assistencial, alertas, automações, auditoria, sincronização e agente

Data: 25/09/2026 (relatório escrito na integração da trilha de banco)
Branch e commits: `claude/kraamzorg-delivery-review-6kzd8q`. `0004_operacao_agente.sql`, `supabase/tests/003_operacao_agente.sql` e `004_estrutura_modelo.sql` entraram no commit de segurança `b123584`.

## Feito

1. **Tabelas do PRD 6.5 ao 6.8**, na ordem que resolve as referências
   (`profissional` primeiro; `mensagem_modelo` antes de `regua_faixa` e
   `termo_alerta`): `profissional`, `documento_profissional`,
   `bloqueio_agenda`, `instrumento`, `consulta_prenatal`, `acompanhamento`,
   `designacao`, `visita`, `registro_atendimento`, `registro_adendo`,
   `anexo_audio`, `relatorio_medico`, `regra_alerta`, `alerta_clinico`,
   `ocorrencia`, `pos_venda`, `automacao`, `automacao_execucao`,
   `mensagem_modelo`, `regua_faixa`, `termo_alerta`, `notificacao`,
   `log_auditoria`, `fila_sincronizacao`, `agente.base_conhecimento`,
   `agente.ingestao_execucao`, `agente_n8n.documentos` e
   `agente_n8n.chat_memoria` (com os índices do PRD).
2. `registro_atendimento` e `registro_adendo` já nascem append-only
   (privilégio revogado mais os gatilhos do P03).
3. Coluna `versao` com gatilho de incremento (`privado.incrementar_versao`)
   nas tabelas da regra 13 do PRD 6.10.
4. View `familia_elegivel_marketing` (PRD 6.9 [v4.2]), `security_invoker`,
   com colunas explícitas (sem `historico_sensivel`) e o filtro do freio.
5. Índices, comentários e RLS ligada.
6. Testes: `003_operacao_agente.sql` (4 asserções: as 53 tabelas de negócio
   do PRD existem com RLS) e a conferência cruzada
   `004_estrutura_modelo.sql` (58 asserções: colunas padrão, gatilhos de
   `atualizado_em`, índice em toda FK, `versao`, append-only também para
   `service_role`, checks do DDL com casos negativos, view de marketing,
   fronteira de privilégio das funções e enums).

## Ficou de fora (e por quê)

- `ocupacao_projetada`: P19 (`0011_ocupacao.sql`).
- Grants e políticas do `n8n_agente`: P21 (`0013_agente_parte1.sql`).
- Imutabilidade de `log_auditoria` e gatilho de auditoria: P05.
- Políticas de todas as tabelas: P07.

## Decisões tomadas nesta sessão

- `anexo_audio.retencao_ate` fica como coluna com comentário da política
  proposta (O-03, 90 dias depois do envio da evolução), sem regra fixa no
  SQL.
- `familia_elegivel_marketing` lista colunas uma a uma, para uma coluna
  nova de `familia` nunca vazar para o marketing por `select *`.

## Mudanças no PRD

Nenhuma.

## Pendências novas ([confirmar], [clínico], terceiros)

- Retenção de áudio (O-03) [confirmar: Leonardo e jurídico].
- Revisão humana do SQL antes do `db push` (parada prevista no PROMPTS.md).

## Como testar

Sem Docker (esta máquina), na raiz do repositório:

```bash
cd supabase/sem-docker
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/iniciar.sh    # sobe o Postgres 16 local (idempotente)
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/testar.sh     # reset do zero + seeds do config.toml + pg_prove
PGPORT=54342 PGDATA=/tmp/kz-pg-final scripts/parar.sh
cd ../..
node supabase/checar-seed.mjs
```

Um arquivo só, depois de um `scripts/resetar.sh` (o `000_harness.sql` cria o
schema `testes` que os outros usam):

```bash
sudo -u postgres pg_prove --host 127.0.0.1 --port 54342 --dbname kraamzorg --username postgres \
  supabase/tests/000_harness.sql supabase/tests/ARQUIVO.sql
```

Com Docker (prova final antes de qualquer `db push`, `supabase/LIGAR.md`):

```bash
supabase start
supabase db reset     # migrations + [db.seed] de supabase/config.toml
supabase test db
```

## Resultado dos invariantes

Reset do zero e suíte inteira na integração da trilha de banco (porta 54342,
`PGDATA=/tmp/kz-pg-final`, seeds de `[db.seed]`): `Files=17, Tests=1971,
Result: PASS`. Invariante 1 (máquinas de estado): `006_maquinas_estado.sql`
verde. Invariante 2 (permissões): `007_permissoes.sql` e `015_crm_apoio.sql`
verdes. Invariante 3 (freio): `009_freio.sql` verde. Invariante 4
(sincronização offline) é do app (P12, `pnpm e2e:offline`) e não foi rodado
nesta integração, que só mexeu no banco.
