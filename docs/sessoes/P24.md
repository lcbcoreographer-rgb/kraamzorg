# P24 · n8n: fluxo 2, Pausar IA e Notificar Equipe

Data: 25/09/2026
Branch e commits: `claude/kraamzorg-delivery-review-6kzd8q`, dentro dos commits de segurança `d584e8c` e `b123584`. Sem commit próprio ainda.

## Feito

1. `n8n/src/fluxo-2-pausar-notificar.mjs`: os 19 nós do PRD 19.3 mais o nó 8a "Subiu para Alerta?", com os nomes do PRD (44 nós no JSON). Onde o PRD descreve um nó como várias chamadas (nó 4, plantão), cada chamada virou um nó; nós Code pequenos juntam o estado depois de cada chamada, porque Postgres e HTTP trocam o item inteiro pela resposta.
   - Nó 8a: classificador que sobe para `saude` ou `perda` volta ao nó 3 antes do registro; a família recebe o texto, o grupo recebe `{mensagem_enviada}` preenchido e o agente recebe `instrucao_saude`.
   - Duplicado só silencia motivo comercial; `saude`, `perda` e `estado_sensivel_escreveu` sempre reenviam ao grupo e ao plantão.
   - Plantão avisado em lote (um envio por número).
   - Sem banco em saúde ou perda, o aviso vai ao `grupoFallbackJid` com o texto do config ("[NÃO REGISTRADO NO SISTEMA] Possível alerta de saúde · {telefone} · \"{texto_familia}\"").
   - Retorno `{ok, handoff_id, instrucao, instrucao_chave, motivo, acao}`.
2. Funções puras em `n8n/src/code/`: `motivos-handoff.js` (motivos, prioridade, ordem de urgência), `pular-classificador.js` (nó 5), `normalizar-entrada-handoff.js` (nó 2), `ler-classificacao-pedido.js` (regras v4.2 do nó 8) e `estado-handoff.js` (demais transições, com o contrato esperado do banco no topo).
3. Build: `codigo-embutido.mjs` junta os imports locais num script só por nó Code; `prompts.mjs` ganhou `carregarPromptSync`; três verificadores novos (`jidSoParaEnviar`, `codeSemImportNemExport`, `autenticacaoSoPorCredencial`).
4. Testes em `n8n/fluxo-2.test.mjs`, importado por `build.test.mjs`: todos os cenários do P24 v2 rodando o JSON gerado no simulador (`n8n/src/lib/simulador.mjs`), mais a estrutura do JSON (nenhum texto de família ou equipe no fluxo, prompt entre as marcas, credenciais só por id e nome do config).
5. Execução num n8n 2.40.6 local, com Postgres, OpenAI, UAZAPI e Redis trocados por nós que avaliam as mesmas expressões: classificador sobe para saúde, banco fora do ar em saúde e `sem_aviso` aceito, sem erro. Repetido na verificação de 25/09, sem erro.

## Ficou de fora (e por quê)

- Importação em homologação e chamadas de teste que põem a mensagem no grupo de teste e a pausa no Redis e no banco (aceite do P24): a instância de homologação e as funções do banco (P21, P22) ainda não existem. Passo a passo em `n8n/IMPORTAR.md`.

## Decisões tomadas nesta sessão

1. Entradas opcionais além da lista do 19.3: `tipo`, `modo`, `telefone`, `alerta_internacao_ativo`, `alerta_emocional_ativo` (e, no P25, `prioridade_minima`, que só sobe a prioridade). Sem elas o fluxo vai pelo caminho mais seguro: sem `modo`, o `sem_aviso` é recusado; ativação ausente vira `alerta_saude`.
2. Troca de motivo comercial segue o 19.3 v4.2: o destino passa ao motivo novo, a maior prioridade e as `opcoes` vão ao banco em `dados._fluxo2`.
3. Salvaguardas além do texto do PRD: com semanas, DPP ou plano de interesse na ficha o classificador não marca não lead; transferência com motivo `saude` ou `perda` vira alerta, nunca é rebaixada.
4. Único uso do jid no banco: `agente.registrar_mensagem` (o Apêndice A resolve a conversa pelo jid). Todas as outras chamadas usam o `conversa_id`, e o verificador `jidSoParaEnviar` garante.
5. Sem banco num pedido que não é alerta: `ok: false`, `instrucao` nula e `instrucao_chave: instrucao_erro`; o fluxo 3 trata.

## Mudanças no PRD

Nenhuma.

## Pendências novas ([confirmar], [clínico], terceiros)

- P21 e P22: `mensagem_sistema` aceitar `instrucao_sem_aviso`; `registrar_handoff` ler `dados._fluxo2` (`prioridade_minima`, `manter_opcoes`, `mensagem_enviada`).
- Sem banco, o texto de alerta não chega à família (o texto mora em `mensagem_modelo` e nenhum texto de família pode ficar no fluxo); só a equipe é avisada pelo grupo de reserva. É o comportamento do 19.4 nó 15 ("banco fora do ar: não responde à família"), registrado aqui para a Edilaine saber.

## Como testar

```sh
node --test n8n/build.test.mjs          # inclui fluxo-2.test.mjs
node n8n/build.mjs --env hml --config /tmp/config.hml.json --saida /tmp/n8n-hml
# importação (Node 24, n8n 2.40.6 fora do repositório)
./validar.sh "/tmp/n8n-hml/kraamzorg-pausar-ia-notificar-equipe (HML).json"
node comparar.mjs "/tmp/n8n-hml/kraamzorg-pausar-ia-notificar-equipe (HML).json"
```

Payloads de exemplo para homologação (entrada do "Quando Chamado", valores fictícios): `{acao: "transferir", conversa_id: "<uuid>", wa_jid: "<jid de teste>", motivo: "reuniao", resumo: "quer conversar com a Edilaine", solicitacao: "quinta ou sexta às 10h", dados: {opcoes: ["quinta 10h", "sexta 10h"]}, origem_chamada: "agente", enviar_texto: false, modo: "vendas"}` e `{acao: "alerta_saude", motivo: "saude", texto_familia: "estou com sangramento", chave_texto: "alerta_saude", enviar_texto: true, origem_chamada: "filtro_termos", ...}`.

## Resultado dos invariantes

Invariantes de banco e app não rodados (a trilha não mexe neles). `node --test n8n/build.test.mjs`: 312 ok. Importação e comparação no n8n 2.40.6 local: sem erro e sem diferença de parâmetro.
