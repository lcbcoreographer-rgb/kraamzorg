import { test } from "@playwright/test";

/**
 * Placeholder do invariante 4 (rede desligada). O P12 substitui este
 * arquivo pelos testes reais de sincronização (preenche offline, religa,
 * confere no banco), junto com src/lib/sync.
 */
test.skip("invariante 4: sincronização offline (preenchido no P12)", async () => {
  // Sem implementação ainda. Ver src/lib/sync (README) e CLAUDE.md, item 22.
});
