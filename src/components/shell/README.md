Casca por papel (P10 item 3): `CascaApp` (barra lateral no computador, abas inferiores no celular) e `CascaEnfermeira` (abas sempre, até 720 px).
`CabecalhoTela` é o título único da tela; `TelaEmConstrucao` é o estado vazio das rotas que o módulo dono ainda vai preencher.
A navegação vem de `src/lib/navegacao`; o ícone de cada item, de `icones-navegacao.tsx`.
