Telas de acesso (P07): entrar, desafio e cadastro do MFA com QR, esqueci a senha, criar senha, convite pela diretoria, `/auth/confirmar` (link do e-mail) e `/sair`.
Fora da casca do app. As Server Actions ficam em `acoes.ts`; os textos em `mensagens.ts`.
No modo demonstração, entrar é um seletor das pessoas fictícias do seed e o MFA aceita o código do protótipo.
