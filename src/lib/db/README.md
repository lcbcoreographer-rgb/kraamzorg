Cliente Supabase de servidor (cookies e RLS) e cliente de serviço (service_role).
O cliente de serviço só roda no servidor, protegido por import server-only.
Preenchido pelo P01 (Supabase local e testes de banco).
