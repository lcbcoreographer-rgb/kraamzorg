Registro único da navegação por papel (PRD 20.4, P10 item 3).

`ROTAS` (caminho, rótulo, ícone e dono de cada tela) e `NAVEGACAO` (abas do celular e grupos da barra lateral de cada papel). A casca, a aba "Mais" e o proxy leem daqui; módulo novo acrescenta a rota aqui e cria só a própria pasta em `src/app`.
